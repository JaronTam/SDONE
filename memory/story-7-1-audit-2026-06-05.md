# Story 7.1 Audit Memory — 2026-06-05

## Key Decisions
- Feedback connections use `isFeedback: true` flag on `Connection` type
- Default formula: `max(0, (capacity - value) / capacity)` — produces multiplier 0→1
- `FormulaEngine.evaluateForConnection()` injects `{ value, capacity, stock_value }` variables
- Simulation tick: Step 2 evaluates feedback formulas, Step 3 applies multiplier to inflow connections
- Feedback connections are SKIPPED in `computeNetFlow` (information flow, not material flow)
- Bezier curve rendering with `FEEDBACK_ARC_OFFSET = 60` for visual arc
- Feedback handle: amber dot at bottom-right of stock, only shown for stocks with source inflow
- `addFeedbackConnection` is idempotent (no duplicate stock→source feedback)

## Files Changed
- GraphState.ts, mutations.ts, FormulaEngine.ts, SimulationEngine.ts
- SceneRenderer.ts, ParticleEngine.ts, InputManager.ts
- RateEditorPanel.ts, main.ts, EventMap.ts
- New: feedback.test.ts (11 tests)

## Test Results
- 651 total tests pass, 0 TypeScript errors